import React from "react";
import { Card, CardBody } from "reactstrap";

const Products = () => {
  return (
    <Card>
      <CardBody>Products list goes here...</CardBody>
    </Card>
  );
};

export default Products;
